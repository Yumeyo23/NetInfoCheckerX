网络综合查询器X

# 其他自定义配置编写指南

查询器X主程序已升级配置文件。基于此，我们给部分以下功能也增加了自定义配置功能，您可以提前按指定格式编写好配置文件，即可在使用对应功能时加载这部分 DIY 内容。

截至11.2609.1.0版本，目前支持以下自定义配置文件：

- `NICX_WebLink.nicxapi`：常用网站。
- `NICX_CustomPing.nicxapi`：延迟测试-自定义节点。
- `NICX_CustomRes.nicxapi`：自定义资源列表（在Ping+、Trace+、NAT类型测试、连接数测试、DNS劫持测试、DNS真选 的下拉框中可用）。

这些配置文件语法与其他部分的配置文件不同。请将配置文件使用 UTF-8 编码，并放在 `NetInfoCheckerX.exe` 旁边。

## 零、AI 编写建议

考虑到大家可能更愿意用 AI 编写配置文件，可[点击这里](https://github.com/Yumeyo23/NetInfoCheckerX/edit/main/3%E5%85%B6%E4%BB%96%E8%87%AA%E5%AE%9A%E4%B9%89%E9%85%8D%E7%BD%AE%E7%BC%96%E5%86%99%E6%8C%87%E5%8D%97.md)下载本 Wiki 完整文档。下载后，把`本文`交给 AI，同时提供你的想法让 AI 编写。

向 AI 说明你希望添加的网站、测试节点或服务器，以及它们需要出现在哪个功能中即可。生成后仍建议检查文件名、英文逗号和分节名称是否正确。

## 一、通用规则

1. 配置文件必须放在主程序 `NetInfoCheckerX.exe` 所在目录，文件名不可修改。
2. 建议使用 UTF-8 编码。
3. 配置文件在打开对应功能窗口时读取。修改后可关闭并重新打开窗口使其生效。
4. 文件不存在时，对应的自定义内容不会加载，但不会影响其他无关功能。
5. 三种配置文件的语法彼此不同，但非常简单。

## 二、常用网站：NICX_WebLink.nicxapi

该配置用于“常用网站”窗口。配置按书写顺序从上到下显示。

### 2.1 分组标题

只有名称、不填写链接的行会显示为分组标题：

```text
🌏 测速网 >>
```

也可以保留英文逗号但让链接为空：

```text
🌏 测速网 >>,
```

### 2.2 网站按钮

每排写入一个网站，网站按钮使用两个参数，中间以**英文逗号**分隔：

```text
按钮名称, 网页链接
```

例如：

```text
测速NET, https://www.speedtest.net/zh-Hans
百度地图, https://map.baidu.com/
12306, https://www.12306.cn/index/
```

左侧文字会显示在按钮上，点击按钮后会把右侧链接交给系统默认浏览器打开。

程序只使用每行第一个英文逗号区分名称和链接，链接中出现的逗号会保留下来。

注意：

- 按钮大小固定不可调，建议标题不多于5个中文字。
- 窗口宽度默认/最小为每行显示 6 个按钮。
- 窗口会根据标题数和按钮行数自动调整高度。同名按钮可以重复出现。
- 空行会被忽略。
- 文件不支持备注。

一个完整示例如下：

```text
🌏 测速网 >>
测速NET, https://www.speedtest.net/zh-Hans
中科大测速, http://test.ustc.edu.cn/

🔍 网络工具 >>
IP125, https://ip125.com/
ITDOG, https://www.itdog.cn/
```



## 三、自定义资源：NICX_CustomRes.nicxapi

该文件为多个功能的下拉列表提供自定义项目。例如，可以设置Ping+、连接数测试的测试目标下拉框内需要显示哪些目标地址，方便后续直接选择并测试，无需每次重复设置。

文件使用“分节 + 每行一项”的格式：

```text
[分节名称]
项目1
项目2
```

项目可以是任意文本。

空行以及以 `#` 或 `;` 开头的行会被忽略，可用于添加说明：

```text
# 这是注释
; 这也是注释
```

分节名称必须使用英文方括号并保持下表中的写法：

| 分节 | 使用位置 | 建议填充内容 |
| --- | --- | --- |
| `[Ping]` | Ping+、Trace+、连接数测试 | 常 Ping 的目标 |
| `[STUN-NAT]` | NAT 类型测试、多出口测试-全能版 | STUN 服务器（可测试 NAT 类型） |
| `[STUN-General]` | Ping+、Trace+、多出口测试-全能版 | STUN 服务器（仅可回显IP、无法测试NAT类型） |
| `[Connection]` | 连接数测试 | 测试连接数的专用目标（或TCP网页服务器） |
| `[DNS]` | DNS 真选 | DNS 及 DoH 地址 |
| `[DNS-TLD]` | DNS 劫持测试 | 用于域名测试的目标域名 |

- 根据资源分类性质，同一个资源可能固定被多个窗口使用，此属性你无法自行修改。

  例如，Ping+ 和 Trace+ 会依次载入 `[Ping]`、`[STUN-General]`；连接数测试会依次载入 `[Connection]`、`[Ping]`。只需修改一处，相关窗口自动使用新内容。请以实际载入内容为准。

- 程序会忽略不存在的分节，并在加载时过滤完全相同的重复项目。

- 每个项目可以是任意文本。Ping+ 和 Trace+ 窗口支持选择混合文本并在开测时自动清洗。

  例如，可以在自定义资源配置中写入行 `阿里 223.5.5.5` ，在Ping+中加载后也可以直接选中，无需手动删除“阿里”汉字，点击开测后会自动删除。

- 项目较多时，如需分组以看的更加清晰，可用任意形式的单行文本充当分组符号，程序会完整加载（例如下面示例中的`==国内DNS==`）。

完整示例：

```text
[Ping]
==国内DNS==
阿里 223.5.5.5
腾讯 119.29.29.29
==国外DNS==
8.8.8.8
1.1.1.1

[STUN-NAT]
stun.example.com

[STUN-General]
stun.example.net

[Connection]
58.22.100.216/8080

[DNS]
223.5.5.5
https://dns.alidns.com/resolve

[DNS-TLD]
example.com
example.net
```



## 四、延迟测试-自定义节点：NICX_CustomPing.nicxapi

该文件用于“延迟测试-自定义节点”，每行必须包含节点名称和一个 IPv4 地址，以**英文逗号**分隔：

```text
节点名称, IPv4地址
```

例如：

```text
阿里, 223.5.5.5
腾讯, 119.29.29.29
电信, 202.103.24.68
```

- 最多读取前 32 个有效节点。
- 目前只接受 IPv4 地址，不接受域名或 IPv6 地址。
- 名称和地址均不可为空。
- 空行会被忽略。
- 该文件不支持分组标题，也不支持 `#`、`;` 等注释行。
- 任意一行缺少英文逗号，或名称、IPv4 地址无效，都会使整份配置加载失败。窗口会提示编辑配置文件。

重启 延迟测试-自定义节点 窗口以重新加载修改后的配置文件。

## 五、常见问题

### 5.1 修改后没有变化

请依次确认：

1. 文件位于 `NetInfoCheckerX.exe` 旁边。
2. 文件名及扩展名完全正确。
3. 使用的是英文逗号 `,`、英文方括号 `[]`，而不是中文标点。
4. 关闭并重新打开了对应窗口。

### 5.2 可以只保留部分内容吗

可以。所有配置文件用不用、怎么用，选择权都在你。

具体文件来说，`NICX_WebLink.nicxapi` 可以只写网站不写标题（甚至只写标题不写网站）；`NICX_CustomRes.nicxapi` 可以只保留需要的分节和内容；`NICX_CustomPing.nicxapi` 可以只写1个节点。

### 5.3 会上传、分享我的自定义配置吗

不会。这些配置完全从本机程序目录读取，除非你主动分享，否则其他人永远不知道你的配置文件。

程序也不会上传获取你的配置给开发者，我没兴趣，拿着也没啥用。
